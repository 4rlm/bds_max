<%= form_for(in_host_neg) do |f| %>
  <% if in_host_neg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(in_host_neg.errors.count, "error") %> prohibited this in_host_neg from being saved:</h2>

      <ul>
      <% in_host_neg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :term %>
    <%= f.text_field :term %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
