<p id="notice"><%= notice %></p>

<h1>In Host Negs</h1>

<table>
  <thead>
    <tr>
      <th>Term</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @in_host_negs.each do |in_host_neg| %>
      <tr>
        <td><%= in_host_neg.term %></td>
        <td><%= link_to 'Show', in_host_neg %></td>
        <td><%= link_to 'Edit', edit_in_host_neg_path(in_host_neg) %></td>
        <td><%= link_to 'Destroy', in_host_neg, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New In Host Neg', new_in_host_neg_path %>
