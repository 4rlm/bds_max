<div class="col">
    <table class="table table-hover">
        <thead>
            <tr>
                <th><%= col_header %></th>
            </tr>
        </thead>
        <tbody>
            <% criteria_table.each do |obj| %>
                <tr>
                    <td><%= obj.term %></td>
                </tr>
            <% end %>
        </tbody>
    </table>
</div>
