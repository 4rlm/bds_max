<p id="notice"><%= notice %></p>

<h1>In Text Negs</h1>

<table>
  <thead>
    <tr>
      <th>Term</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @in_text_negs.each do |in_text_neg| %>
      <tr>
        <td><%= in_text_neg.term %></td>
        <td><%= link_to 'Show', in_text_neg %></td>
        <td><%= link_to 'Edit', edit_in_text_neg_path(in_text_neg) %></td>
        <td><%= link_to 'Destroy', in_text_neg, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New In Text Neg', new_in_text_neg_path %>
