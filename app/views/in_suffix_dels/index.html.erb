<p id="notice"><%= notice %></p>

<h1>In Suffix Dels</h1>

<table>
  <thead>
    <tr>
      <th>Term</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @in_suffix_dels.each do |in_suffix_del| %>
      <tr>
        <td><%= in_suffix_del.term %></td>
        <td><%= link_to 'Show', in_suffix_del %></td>
        <td><%= link_to 'Edit', edit_in_suffix_del_path(in_suffix_del) %></td>
        <td><%= link_to 'Destroy', in_suffix_del, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New In Suffix Del', new_in_suffix_del_path %>
