<%= form_for(in_suffix_del) do |f| %>
  <% if in_suffix_del.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(in_suffix_del.errors.count, "error") %> prohibited this in_suffix_del from being saved:</h2>

      <ul>
      <% in_suffix_del.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :term %>
    <%= f.text_field :term %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
