<p id="notice"><%= notice %></p>

<h1>In Host Dels</h1>

<table>
  <thead>
    <tr>
      <th>Term</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @in_host_dels.each do |in_host_del| %>
      <tr>
        <td><%= in_host_del.term %></td>
        <td><%= link_to 'Show', in_host_del %></td>
        <td><%= link_to 'Edit', edit_in_host_del_path(in_host_del) %></td>
        <td><%= link_to 'Destroy', in_host_del, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New In Host Del', new_in_host_del_path %>
