<p id="notice"><%= notice %></p>

<h1>In Text Pos</h1>

<table>
  <thead>
    <tr>
      <th>Term</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @in_text_pos.each do |in_text_po| %>
      <tr>
        <td><%= in_text_po.term %></td>
        <td><%= link_to 'Show', in_text_po %></td>
        <td><%= link_to 'Edit', edit_in_text_po_path(in_text_po) %></td>
        <td><%= link_to 'Destroy', in_text_po, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New In Text Po', new_in_text_po_path %>
